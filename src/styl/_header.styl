header
  color #fff
  height 100%
  max-height 100vh
  background main
  position absolute
  width 100%

  &.static
    height 12.5rem

    .logo
      .name
        font-size .875rem
      .text
        font-size .563rem

  &.top
    max-height 12.5rem
    transition max-height 2.5s cubic-bezier(.19,1,.22,1) 2s

    .logo
      .name
        animation-name fadeIn, fontSizeLogoName
        animation-duration .8s, .8s
        animation-timing-function ease-out
        animation-fill-mode forwards
        animation-delay 0s, 2s

      .text
        animation-name fadeIn, fontSizeLogoText
        animation-duration .8s, .8s
        animation-timing-function ease-out
        animation-fill-mode forwards
        animation-delay .8s, 2s

  .header-inner
    display flex
    justify-content center
    align-items center
    height 100%

  .logo
    .name
      opacity 0
      font-size 1.9rem
      text-align center

      animation fadeIn .8s ease-out forwards

    .text
      opacity 0
      font-size 1.55rem
      text-align center

      animation fadeIn .8s ease-out forwards
      animation-delay .8s
