{
  "name": "marcus",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "npm run updateInfo && ./node_modules/.bin/harp server src",
    "build": "npm run build:imgs && ./node_modules/.bin/harp compile src dist",
    "build:imgs": "npm run min-imgs && npm run update-info && npm run opt-imgs",
    "min-imgs": "node generateMinImages",
    "update-info": "node updateImageData",
    "opt-imgs": "./node_modules/.bin/imageoptim -d src/assets/images --verbose",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Nipher/marcus.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/Nipher/marcus/issues"
  },
  "homepage": "https://github.com/Nipher/marcus#readme",
  "devDependencies": {
    "easyimage": "^2.1.0",
    "harp": "^0.20.1",
    "imageoptim-cli": "^1.14.8",
    "kouto-swiss": "^0.11.14",
    "watch-cli": "^0.2.1"
  }
}
