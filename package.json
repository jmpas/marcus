{
  "name": "marcus",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "yarn update-img-data && yarn build:tpl && webpack-dev-server",
    "build": "yarn clean:dist && yarn build:imgs && yarn build:tpl && webpack -p",
    "build:tpl": "pug -o dist -O src/_images.json src/index.pug",
    "build:imgs": "yarn copy-imgs && yarn min-imgs && yarn update-img-data && yarn opt-imgs",
    "min-imgs": "rm -rf dist/images/*.min.* && resize dist/images/* -w 400 -h 400 -s \".min\" -o dist/images",
    "update-img-data": "node update-image-data",
    "opt-imgs": "imageoptim -d dist/images --verbose",
    "copy-imgs": "cp -R src/images dist && cp src/favicon.ico dist && cp src/site-image.png dist",
    "clean:dist": "rm -rf dist && mkdir -p dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Nipher/marcus.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/Nipher/marcus/issues"
  },
  "homepage": "https://github.com/Nipher/marcus#readme",
  "devDependencies": {
    "css-loader": "^0.26.1",
    "easyimage": "^2.1.0",
    "extract-text-webpack-plugin": "^2.0.0-beta",
    "image-webpack-loader": "^3.2.0",
    "imageoptim-cli": "^1.14.8",
    "kouto-swiss": "^0.11.14",
    "pug-cli": "^1.0.0-alpha6",
    "resize-cli": "^0.0.4",
    "script-loader": "^0.7.0",
    "serve": "^3.2.3",
    "static-server": "^2.0.4",
    "style-loader": "^0.13.1",
    "stylus": "^0.54.5",
    "stylus-loader": "^2.4.0",
    "webpack": "^2.2.1",
    "webpack-dev-server": "^2.3.0"
  },
  "dependencies": {
    "classie": "^1.0.0",
    "holderjs": "^2.9.4",
    "imagesloaded": "^4.1.1",
    "masonry-layout": "^4.1.1",
    "photoswipe": "^4.1.1"
  }
}
